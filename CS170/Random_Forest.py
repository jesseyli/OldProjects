class node(object):
    def __init__(self, feature, threshold):
        self.left = []
        self.right = []
        self.f = feature
        self.t = threshold
    
def classify(T, bulkData, entry):
    
def buildTree(data):
    if len(data) == 1:
        
