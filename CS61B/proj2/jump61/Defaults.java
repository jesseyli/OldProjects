package jump61;

import static jump61.Color.*;

/** Collected constants used as default or initial values.
 *  @author Jesse Li
 */
class Defaults {

    /** Current version designator. */
    static final String VERSION = "Jump61 2.0";

    /** Default board size. */
    static final int BOARD_SIZE = 6;

    /** Default number of spots on a space. */
    static final int SPOT_NUM = 0;

    /** Default color of spaces. */
    static final Color COLOR = WHITE;

    /** Default AI depth for minmax. */
    static final int DEPTH = 4;
}
